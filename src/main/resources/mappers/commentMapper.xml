<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring_boot_final.project.dao.ICommentDAO">

    <!-- Create -->

    <insert id="createComment" parameterType="com.spring_boot_final.project.model.CommentVO">
        <![CDATA[
        INSERT INTO comment (noteId, userId, comment)
        VALUES (#{noteId}, #{userId}, #{comment})
        ]]>
    </insert>

    <!-- Read -->

    <select id="selectCommentList" parameterType="int" resultType="com.spring_boot_final.project.model.CommentVO">
        <![CDATA[
        select c.commentId,
               c.noteId,
               c.userId,
               c.comment,
               c.commentLike,
               c.commentCreatedDate,
               u.userNickname
        from comment c,
             user u
        where c.noteId = #{noteId}
          and c.userId = u.userId
          and c.pageViewState = 'POST'
        order by c.commentCreatedDate desc;
        ]]>
    </select>

    <select id="selectComment" parameterType="int" resultType="com.spring_boot_final.project.model.CommentVO">
        <![CDATA[
        select *
        from comment
        where commentId = #{commentId};
        ]]>
    </select>

    <!-- Update -->

    <update id="updateComment" parameterType="com.spring_boot_final.project.model.CommentVO">
        <![CDATA[
        UPDATE comment
        SET comment=#{comment}
        WHERE commentId = #{commentId}
          AND userId = #{userId}
        ]]>
    </update>

    <!-- Delete -->

    <update id="deleteComment" parameterType="com.spring_boot_final.project.model.CommentVO">
        <![CDATA[
        UPDATE comment
        SET pageViewState='DELETE'
        WHERE commentId = #{commentId}
          AND userId = #{userId}
        ]]>
    </update>


</mapper>